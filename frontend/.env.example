# Backend origin for the API client. Leave empty to stay same-origin and use the
# Vite dev proxy (see vite.config.ts). Set this in production to the backend URL.
VITE_API_URL=

# Where the Vite dev server proxies /api requests during local development.
VITE_PROXY_TARGET=http://localhost:8000
